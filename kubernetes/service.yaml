apiVersion: v1
kind: Service
metadata:
  name: kubenexus
  namespace: platform-mcp
  labels:
    app.kubernetes.io/name: kubenexus
spec:
  type: ClusterIP
  selector:
    app: kubenexus
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
